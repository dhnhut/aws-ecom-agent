# Packaged template produced by `aws cloudformation package` (build artifact)
template.packaged.yaml

# Stack outputs (account-specific, regenerated by scripts/deploy.sh)
cfn-outputs.json

# Leftovers from the retired CDK app
node_modules/
cdk.out/
cdk-outputs.json
